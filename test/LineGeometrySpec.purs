module Test.LineGeometrySpec where

import Prelude
import Test.Spec (Spec, describe, it)

lineGeometrySpec :: Spec Unit
lineGeometrySpec = do
  describe "LineGeometrySpec" do
    it "should divide" do
      pure unit
